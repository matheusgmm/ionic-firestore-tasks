import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicFirestoreTasks',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
